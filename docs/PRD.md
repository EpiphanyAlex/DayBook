---
title: Daybook 总 PRD — 产品范围、成功标准、非目标与里程碑地图
status: ready
owner: "@maintainer"
date: 2026-08-08
version: v0.6
---

# Daybook 总 PRD

> Daybook（日簿）是一个跑在本地的 AI 桌面助手，帮用户把过去一段时间的钱和事补记回来。
> 本文定义**做什么、做到什么算成功、明确不做什么**，以及里程碑与各 sub-PRD 的对应关系。**范围变更必须先改本文。**
> 技术选择的完整论证在 [`docs/adr/`](./adr/)；术语定义在 [`docs/CONTEXT.md`](./CONTEXT.md)；系统结构在 [`docs/architecture.md`](./architecture.md)。

---

## 1. 问题

记账工具的设计前提是「交易发生时你会记一笔」。

**这个前提在实际使用中几乎不成立。** 人在付款当下不会掏出 app 记账——排队、赶路、和人说着话。于是记录被推迟，推迟到某个时刻用户坐下来，面对一堆已经模糊的痕迹，试图把过去一两周重建出来。

**现有工具在这一步把用户丢掉了。** 它们为「当场记录」持续优化——快捷输入、桌面小组件、快捷指令、语音速记——却把「事后重建」原样留给用户手工完成：翻银行流水、对截图、逐条敲进去、算汇率、查这笔到底是什么。**补记一次的成本远高于当场记十次**，于是用户放弃，工具沦为装了没用的那一个。

Daybook 的设计前提相反：**默认你是事后补的。** 产品的全部工作是把「重建过去」这件事的成本压到可接受。

### 1.1 录入摩擦是痛点的另一半

上面讲的是「事后补记成本高」。但痛点还有一半在当下：**传统工具里创建一条记录要点好几步**——选账户、选分类、填金额、填商户、填日期、保存。摩擦本身就是人放弃记账的原因之一。

竞品也在打这一半，方式是**做更快的表单**：小组件、快捷指令、语音速记。**我们的解法不同——不用表单。**

| | 竞品的语音速记 | Daybook |
|---|---|---|
| 说一次话 | 记**一笔** | 说一段话，agent 拆成**多笔** |
| 之后 | 仍要确认那一个表单 | **一次批量确认** |
| 跨实体 | 不支持 | 一句话里的交易和事项一起处理（[ADR-0003 §2](./adr/0003-agent-runtime-and-pluggable-backend.md) 的论据场景） |

「今天吃饭 180，明天交房租，上周那 400 是给我妈买茶叶」——一句话跨多笔、跨实体，说完一次确认完。**竞品做的是更快的表单，我们做的是没有表单。**

由此确定三件事：

- **AI 的角色是考古学家，不是输入框。** 从截图等痕迹里把过去还原出来，摆到用户面前，用户只负责审。核心动作是**重建**，不是**录入**。
- **表单不是主路径。** 手工填表只作为 agent 不可用时的兜底与旧记录的编辑入口（[`04-transactions`](./prd/04-transactions.md) §3.5），UI 不给它显眼的入口。
- **产品是低频、重型的**——一两周一次坐下来处理完，不是日常高频工具。但**单条快速录入随时可用**，这两件事不矛盾：低频指的是「补记一整段时间」这个仪式，不是禁止你随口记一笔。

## 2. 用户

**主用户：想记账、但坚持不下来的人。**

放弃的原因通常不是不想记，而是补记的成本太高。这不是一个小众人群——它是记账工具共同的流失点。

**不需要用户具备任何特殊属性。** 「当场记不了、只能事后补」不是用户细分，是使用常态；把它当细分来定位会把问题写小。

**痛感强度随三个维度递增**：账户数 → 支付渠道数 → 币种数。这三项越多，手工整理越不可行，产品的价值越明显。因此多账户 / 多渠道 / 多币种用户是**最先被救到的人群**，也是本项目的**首要验证场景**（多账户 + 多渠道 + 双币种）。

**但这是压力测试场景，不是市场边界。** 产品不依赖任何特定国家、银行或币种——理由见 §3.1：解析能力不认识具体格式，因此天然与地域无关。

v1 不服务：多人协作、家庭共享账本、企业报销。

## 3. 产品原理

三条互相支撑的判断，解释「为什么这个产品现在才可能，以及为什么只能长成这个形状」。

### 3.1 用户自带 AI 额度 → 边际成本为零 → 可以对任意来源做重解析

传统记账 app 必须为每家银行、每个支付平台写**专用解析器**。这条路线有三个固有缺陷：格式改版就挂、长尾永远覆盖不到、**每进入一个新国家都要重做一遍**。按 API 计费的竞品即使用了模型，算完 token 账也会退回去写解析器。

自带额度让「对着任意截图直接说读吧」成为可能。**这条能力天然与银行、币种、国家无关——因为它根本不认识具体格式**，也就无从被格式局限。多币种、多渠道、任意版式都是这一条的自然结果，不需要为它们单独设计。

### 3.2 用户已安装的 agent CLI 就是成熟的 agent runtime

Claude Code、Codex 本身已具备多轮推理、工具调用、上下文管理。产品需要的是「下达任务、它自己编排」，而不是「一个能调 API 的输入框」。

这也是**必须做成桌面应用**的理由：要调用本机登录态、本机进程、本机文件（[ADR-0003](./adr/0003-agent-runtime-and-pluggable-backend.md)）。

### 3.3 本地优先，无账号、无后端

账目与日程极私密。数据不出本机 → 隐私天然成立、无各国合规问题、零服务器成本。这也是一个人能长期维护的唯一形态（[ADR-0001](./adr/0001-local-first-desktop-platform.md)）。

## 4. 数据可信性：AI 永不直接写入账本

视觉模型会把 168 读成 1680。账本与普通 AI 应用的差别在于错误的性质：**账本错一个数字，用户信任就永久归零，且不可逆**——一旦用户开始回头核对原始截图，产品声称节省的时间就全部吐了回去。

四道闸门，缺一不可，且都由代码强制而非由 agent 自觉遵守（完整论证与实现要求见 [ADR-0002](./adr/0002-ai-never-writes-directly.md)）：

| 闸门 | 要求 | 作用 |
|---|---|---|
| **草稿区** | AI 只写 `draft_*` 表，人确认才由确认动作写事实表 | 任何绕过草稿区的写入路径都是缺陷 |
| **证据链** | 每条草稿挂 `source_id`（哪张截图/哪个文件）+ 原文片段；审核界面原文与解析结果并排 | 把用户的负担从「判断 AI 对不对」换成「扫一眼原文」 |
| **总额交叉校验** | 从一个来源拆出的条目合计，必须与该来源自身声明的合计/余额核对；不符时显式报警并阻止批量入库 | **唯一能在无人工介入下捕获错误的机制，优先级高于解析准确率本身** |
| **审计日志** | append-only，记「谁 / 何时 / 把什么改成了什么」 | 出错时能回溯到具体那一次写入 |

## 5. v1 范围

**原则：「交易」做深，「事项」做薄。**

### 5.1 深：交易

截图导入 → 解析重建 → 证据链 + 总额校验 → 审核 → 入库 → 基础回顾。**做到能真正替代手工流程。**

- 多图批量导入，跨图去重
- **语音/文字一句话记多笔**（§1.1）：说一段话 → agent 拆成 N 条草稿 → 一次批量确认。语音在本机转写，v1 用 macOS 系统听写（[ADR-0005](./adr/0005-voice-and-system-integration.md)）
- 多币种：存**原币金额 + 本位币金额 + 当时汇率**三元组（各带币种标识），不要求用户手算（[ADR-0004](./adr/0004-data-model-sqlite-integer-money.md)）。**本位币可切换**，逐笔冻结
- 商户、分类、渠道
- **审核界面**：密集表格、原文并排、异常项排最前、默认全选、键盘走完。判定标准是 **40 笔 30 秒审完**——整个产品省下的时间全兑现在这一屏
- 基础回顾：这段时间钱花在哪了

### 5.2 薄：事项

一条记录走完整生命周期：`backlog（无日期）→ 排到某天 → 完成（带实际时长）→ 未完成退回 backlog`。**「待办」和「时间日志」是它的两端，不是两个功能。**

- backlog 列表、一句话批量丢入
- 拖到某天、记完成时段
- 能回答「这周时间花哪了 / 什么没做完」

### 5.3 贯穿

- **时间轴是共同骨架，但 UI 分两个视图**——不在同一张日历里混着显示钱和时间
- **记忆系统存规则，不存对话**：商户→分类映射、用户的每次纠正、个人语境词表、语音专有名词表。这是产品唯一的复利——第一个月 40 条改 8 条，半年后 40 条改 1 条
- **交互形态是「对话下指令 + 界面做审核 + 界面看回顾」**，不是聊天框包打天下。语音只用于输入，不用于审核（无法用语音表达「第 7 条金额改成 168」）

## 6. 非目标（v1 明确不做）

| 类别 | 不做的东西 | 说明 |
|---|---|---|
| 事项功能 | 提醒、重复任务、子任务、优先级算法、番茄钟 | 「薄」的字面含义 |
| 采集 | 弱信号采集（git / 日历 / 浏览器历史 / 屏幕使用时间） | 未来做，但必须逐项授权、默认全关（[ADR-0005](./adr/0005-voice-and-system-integration.md)）；届时应接 [ActivityWatch](https://github.com/activitywatch/activitywatch) 而不是自建 |
| 闭环 | 意图↔事实闭环（待办「交房租 2000」被账单里的 2000 转账核销） | v2 方向，见 §11 |
| 数据 | 历史数据导入 | |
| 平台 | 手机端、Windows/Linux 发行 | Tauri 保留后路，v1 只做 macOS |
| 形态 | 任何形式的同步、账号、后端、变现 | 与「本地优先」是同一条约束 |
| 代码 | v1 写任何 Swift | 语音用 macOS 系统听写；sidecar 推 v1.1（[ADR-0005](./adr/0005-voice-and-system-integration.md)） |

## 7. 成功标准

- **一个月**：用它补过 3 次账，**且没有一次因为不放心而回去翻原始截图核对**。开始复核即意味着失败。
- **终点**：不再打开原来的记账软件和日程软件。
- **审核界面单点指标**：40 笔 30 秒审完。

## 8. sub-PRD 地图

**sub-PRD 按能力切，里程碑按时间切**——两个正交维度。索引与状态总览在 [`docs/prd/INDEX.md`](./prd/INDEX.md)。

| sub-PRD | 覆盖 |
|---|---|
| [`00-foundation`](./prd/00-foundation.md) | 数据层、SQLite schema、迁移、错误契约 |
| [`01-agent-runtime`](./prd/01-agent-runtime.md) | MCP server（`rmcp`）、agent 启动器、可插拔后端接口 |
| [`02-ingest`](./prd/02-ingest.md) | 截图导入、`sources` 落库、解析编排 |
| [`03-review`](./prd/03-review.md) | 草稿区、证据链、总额校验、审核界面 |
| [`04-transactions`](./prd/04-transactions.md) | 交易实体、多币种三元组、分类、回顾 |
| [`05-items`](./prd/05-items.md) | 事项实体（backlog → 排期 → 完成时长） |
| [`06-memory`](./prd/06-memory.md) | 记忆规则（商户映射、纠正、语境词表） |
| [`07-eval`](./prd/07-eval.md) | agent 解析质量的评测集、评分器、回归门槛与夹具 |

## 9. 里程碑地图

| 里程碑 | 涉及 sub-PRD | 判定标准 |
|---|---|---|
| **M0** 端到端点亮 | [`00`](./prd/00-foundation.md) + [`01`](./prd/01-agent-runtime.md) + [`02`](./prd/02-ingest.md) + [`03`](./prd/03-review.md) 各取最小切片 | 拖一张截图 → agent 读 → 经 MCP 入库 → 列表显示。端到端跑通即可，不要求好看 |
| **M1** 审核界面 | [`03`](./prd/03-review.md) 做深 | 40 笔 30 秒审完 |
| **M2** 批量与多币种 | [`02`](./prd/02-ingest.md) + [`04`](./prd/04-transactions.md) | 一次处理真实的 10 天（多图批量、跨图去重、多币种 + 历史汇率） |
| **M3** 事项与记忆 | [`05`](./prd/05-items.md) + [`06`](./prd/06-memory.md) | backlog → 拖到某天 → 记时长；纠正能沉淀成规则 |
| **M4** 可插拔与打包 | [`01`](./prd/01-agent-runtime.md) 补全 | 别人能跑起来（可插拔 agent 后端补全 + 打包 + README） |

### 9.1 为什么 M0 优先

**M0 验掉产品最大的未知数**：视觉模型读真实银行与支付账单截图到底准不准。这个问题的答案决定整个产品是否成立，必须最先撞。

验证样本取最难的组合（多渠道、中英文混合、双币种），因为它对解析能力的压力最大——过得了这一关，普通单账户场景不构成风险。

**M0 同时验第二个未知数**（2026-08-08 加入）：**一段口述能不能被可靠地拆成多笔**。§1.1 把录入摩擦确认为痛点的另一半，而「一句话拆多笔」是我们相对竞品语音速记的唯一差异——它和视觉解析一样是未经验证的能力，同样该在 M0 撞。

两个未知数共用同一条下游链路（草稿 → 闸门 → 审核 → 入库），所以第二个的边际成本很低：一列 `sources.kind`、一个输入框、一份提示词模板。

### 9.2 M0 各 sub-PRD 取哪一片

M0 天生横跨多份 sub-PRD——walking skeleton 就是这样。各份取最小切片：

| sub-PRD | M0 切片 | M0 **不**做 |
|---|---|---|
| [`00-foundation`](./prd/00-foundation.md) | SQLite 打开 + 迁移运行器 + `sources` / `draft_transactions` / `transactions` / `audit_log` 四张表 + 错误契约 | 完整 schema、事项相关表、记忆表 |
| [`01-agent-runtime`](./prd/01-agent-runtime.md) | 起 MCP server（stdio）+ spawn `claude -p` + **四个工具**：`list_pending_sources` · `read_source` · `draft_transaction` · `report_source_total`（清单见 [01 §3.2](./prd/01-agent-runtime.md)） | `draft_item` / `query_memory`（目标表 M0 未建，推 M3）、可插拔后端的第二个实现、子 agent 上下文隔离 |
| [`02-ingest`](./prd/02-ingest.md) | 拖入单张 PNG/JPEG 截图 → 落 `sources` + 证据文件 → 触发一次解析。**外加 `kind = utterance` 来源**：一段口述/文字 → agent 拆成 N 条交易草稿 | HEIC / PDF（推 M2）、多图批量、跨图去重、文件监听 |
| [`03-review`](./prd/03-review.md) | 一个能看到草稿、**看到每条的原文片段与声明合计**、并逐条/批量确认入库的最朴素列表；四道闸门全部生效 | 证据图面板与**区域高亮**、键盘流、异常前置排序、虚拟滚动 |

> **M0 的语音只记交易。** `draft_items` / `items` 是 M3 的表（[00 地基 §3.6](./prd/00-foundation.md)），所以 M0 里 agent 听到「明天交房租」这类**事项**内容时**必须明确回一句「这条我现在还记不了」**，不得静默丢弃。把事项表提前到 M0 会显著扩大 M0，明确不做。
>
> **闸门 3 对 utterance 天然失效**：一段口述没有「声明合计」，`declared_total_*` 恒为空，总额校验结果恒为 `unavailable`（[03 审核 §3.3](./prd/03-review.md)）。语音草稿只受草稿区、证据链、审计三道闸门保护——这是已知且接受的代价，`03` 的异常前置排序会把它们单独提一档。

> **`report_source_total` 为什么在 M0**：没有它就没有 `sources.declared_total_minor`，[总额交叉校验](./adr/0002-ai-never-writes-directly.md)（闸门 3）无从成立。M0 的目的是验证端到端链路**连同四道闸门**是否立得住，抽掉一道闸门的 M0 验不出该验的东西。
>
> **`03` 的「原文并排」M0 就要有**：`evidence_text` 是草稿表的一个字符串列，在列表里多渲染一列即可，成本近乎为零，而它是闸门 2 的最小兑现。**M0 推迟的是证据图面板与区域高亮**（[03 §5](./prd/03-review.md) R1 的 spike 对象），不是原文本身。

### 9.3 M0 的端到端判定：`scripts/verify-m0.mjs`（**待建**）

[`01`](./prd/01-agent-runtime.md)、[`02`](./prd/02-ingest.md)、[`03`](./prd/03-review.md) 三份 sub-PRD 都把这个脚本列为验收标准，因此**它验什么必须在这里定义**——否则 M0 的判定标准就是个黑盒（2026-08-07 M0 开工评审补定）。

脚本在一个**临时数据目录**上跑完整链路，退出码非零即失败：

| # | 检查 | 判据 |
|---|---|---|
| 1 | 迁移 | 空目录启动后四张 M0 表存在，`user_version` 等于最大迁移号 |
| 2 | 导入 | 投入一张固定的测试截图 → `sources` 恰好一行，`state = imported`，证据文件存在且 SHA-256 与输入相等 |
| 3 | 幂等 | 同一文件再投一次 → 仍是一行，返回 `deduplicated: true` |
| 4 | 解析 | 触发解析 → `state` 走到 `parsed`，`draft_transactions` 行数 > 0，**每行 `source_id` 与 `evidence_text` 非空** |
| 5 | 审计 | 每条草稿对应一条 `actor = "agent"` 的 `audit_log` |
| 6 | 闸门 | 篡改一条草稿金额使合计不符 → 批量确认返回 `review.total_mismatch`，`transactions` 仍为空 |
| 7 | 确认 | 改回正确金额 → 批量确认成功，`transactions` 行数等于草稿数，每行 `source_draft_id` 指回原草稿且草稿 `consumed_at` 非空 |
| 8 | 隔离 | 全程 `transactions` 的写入只发生在第 7 步（确认动作），MCP 工具调用期间恒为空 |
| 9 | **语音路径** | 投入一段固定文本（模拟转写结果）→ `sources` 多一行且 `kind = utterance`、`declared_total_*` 为空 → 拆出 ≥2 条交易草稿，每条 `evidence_text` 是该文本的对应片段 → 总额校验结果为 `unavailable`（**不是 `failed`**）→ 逐条确认可入库、批量确认被拒 |

**第 4、9 步依赖真实 agent CLI**：脚本在检测不到可用后端时**跳过 4–9 并以非零退出**，明确报「M0 端到端需要已安装并登录的 agent CLI」——**不静默降级为通过**。

第 9 步用固定文本而非真实语音：转写由 macOS 系统听写完成、音频不出本机、应用零代码（[ADR-0005 §1](./adr/0005-voice-and-system-integration.md)），所以脚本的被测对象是「文本 → 多笔草稿」这一段，转写本身不在测试范围。

## 10. 竞争格局（调研于 2026-08-06）

**「AI 记账」本身不是差异化。** 国内移动端已是红海——账明、一木记账、咔皮记账、AI 记账本、百事 AA 记账，「AI 截图记账 + 语音记账 + 对话分析」全部有人做且免费。

但现有工具（无论国内外）共享三个结构性局限：

1. **都为「当场记录」优化**，把事后重建留给用户手工完成（§1）
2. **语音速记止步于「更快的表单」**——说一笔、填一个表、确认一次。没有「一句话拆成多笔、跨实体、一次批量确认」这一层（§1.1）
3. **都依赖专用解析器**，因而与特定国家的银行/支付平台绑定——这也是为什么国内 app 读不懂澳洲银行对账单、海外 app 不认识微信支付宝。**这不是产品的市场划分，是解析器路线的必然结果**（§3.1）

**海外开源则集体回避 AI**：Actual Budget（本地优先、有 AI 查询）、Firefly III（**官方立场就是「不要黑盒机器学习」**）、ezBookkeeping、CurioPay。这反证了「AI 不敢写账本」是一道真实的坎——本项目的证据链 + 总额校验正是绕过它的路（§4）。

**时间追踪**：[ActivityWatch](https://github.com/activitywatch/activitywatch)（开源、本地优先、跨平台、可写自定义 watcher）是弱信号采集的现成基础设施，**未来应接它而非自建**；它只做「自动采集」不做「人工补录」，也不碰钱——中间那层「把机器活动翻译成人类事项」正是本项目该加的价值。

**结构性差异（对手做不到，不是包装）**：

- 竞品商业模式依赖云，「数据不出本机」做了就没饭吃
- 竞品自付 token 成本，做不起重解析，只能退回专用解析器
- 竞品是记账公司，不做钱与时间同轴

## 11. v2 方向（不进 v1）

待办写着「这周交房租 2000」，账单里出现一笔 2000 转账 → 待办自动打勾、预算自动核销。**意图与事实互相印证**——这是分成两个 app 做不到的事，也是「一个 app」的真正理由。

v1 只需保证数据形状（一条时间轴 + 两个实体）不挡住这条路。

## 12. 已知局限

1. **产品压缩的是整理成本，不是记录意愿。** 用户仍需在当下保留痕迹（截图、留存账单）。做不到这一步的用户，本产品救不了。
2. **时间日志做不到精确时段。** 钱有客观痕迹，时间没有，只能粗粒度——好在时间容忍模糊，钱不容忍。
3. **用户订阅的用量限制是真实约束。** 核心操作（多图 + 长上下文 + 多轮推理）恰好最耗额度。

### 平台风险

Anthropic 对第三方 agent 使用订阅额度的政策反复过：2026-04-04 封禁 → 05 月改为 Agent SDK credits（按 API 价计费）→ **06-15 暂停该改动**（当前 `claude -p` 与第三方应用仍走订阅额度）。方向明确但未落地。

对策是可插拔后端接口（[ADR-0003](./adr/0003-agent-runtime-and-pluggable-backend.md)）。**这是工程对策，不进产品叙事。**

## 13. 开放问题

| # | 问题 | 影响 | 谁来决 / 何时 |
|---|---|---|---|
| P1 | 历史汇率数据从哪来——「数据不出本机」下不能调在线汇率 API，只能靠截图里的已换算金额 + 手工录入基准 | [`04-transactions`](./prd/04-transactions.md) | M2 立 spike，产品决定 |
| ~~P2~~ **已关闭（2026-08-07）** | 本位币是固定单一还是可切换 | [`00-foundation`](./prd/00-foundation.md) schema、[`04-transactions`](./prd/04-transactions.md) | **结论：可切换**（产品决定）。固定单一本位币等于在 schema 层重新引入地域假设，与 §3.1「解析能力与国家/币种无关」矛盾。切换语义按 [ADR-0004](./adr/0004-data-model-sqlite-integer-money.md)「折算冻结在交易发生那一刻」推导：`base_currency` 逐笔存储、确认时冻结，切换只影响新交易，不改历史行；跨切换点的回顾按本位币分组，不静默相加。详见 [00 地基 §3.4](./prd/00-foundation.md) 与 [04 交易 §3.1/§3.6](./prd/04-transactions.md) |
| P3 | 证据截图的存储上限与清理策略（长期使用会累积到 GB 级） | [`02-ingest`](./prd/02-ingest.md) | 真实使用出现容量问题时 |
| P4 | 开源许可选型 | 整个仓库 | M4 打包前，产品决定 |

---

### 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-08-06 | 初版：问题与洞察、用户与场景、三个支点、四道闸门、v1 范围（交易深 / 事项薄）、非目标、成功标准、sub-PRD 地图、M0–M4 里程碑与 M0 切片划分、竞品调研、开放问题 P1–P4 |
| v0.6 | 2026-08-08 | **公开仓库去个人化**（仓库转为公开）：§2 与 §9.1 的验证场景去掉具名账户与币种组合，改述为「多账户 + 多渠道 + 双币种」——**能力描述不变，只是不再指向具体的人**；§13 待决与本表的决策署名统一为「产品决定」，去掉工具与会话指代；`owner` 改为 `@maintainer`。**§10「国内 app 读不懂澳洲银行对账单」保留**——那是论证「专用解析器路线必然地域绑定」的行业观察，不是账户清单 |
| v0.5 | 2026-08-08 | **设计评审结论落地。** ① **§1.1 新增「录入摩擦是痛点的另一半」**——评审指出痛点包含「传统创建一条记录要点好几步」，这**推翻了 v0.1 从立项阶段继承的「痛点不是记一笔要点五下」**；同时把差异化说准：竞品的语音速记是「更快的表单」，我们是「一句话拆多笔、跨实体、一次批量确认」，§10 相应加第 2 条局限。② **语音记账进 v1 且进 M0**：§5.1 新增、§9.1 确认 M0 有第二个未知数（一段口述能否可靠拆多笔）、§9.2 M0 切片加 `kind = utterance` 并写明「M0 语音只记交易，遇事项内容必须明说记不了」与「闸门 3 对 utterance 天然失效」、§9.3 新增第 9 步。③ **§1 由两件事扩为三件**，新增「表单不是主路径」（手工填表降级为兜底）。④ §8 新增 [`07-eval`](./prd/07-eval.md)。⑤ 相关 ADR：[ADR-0006 smart agent, dumb tools](./adr/0006-smart-agent-dumb-tools.md)、[ADR-0007 本地可观测性与日志分级](./adr/0007-local-observability-and-log-tiers.md) |
| v0.4 | 2026-08-07 | **随 M0 四份 sub-PRD 开工评审同步。** ① 新增 **§9.3 `scripts/verify-m0.mjs` 的检查项定义**（8 步 + 无可用后端时非零退出、不静默降级）——[01](./prd/01-agent-runtime.md)、[02](./prd/02-ingest.md)、[03](./prd/03-review.md) 三份都把该脚本列为验收标准却无人定义它验什么，M0 的判定标准不能是黑盒。② **§9.2 M0 切片表修正三处**：`01` 的工具数从「两个」改为**四个**并列名（原文与 [01 §3.2](./prd/01-agent-runtime.md) 不一致；`report_source_total` 必须在 M0，否则闸门 3 无从成立）；`02` 明确 M0 只收 PNG/JPEG；`03` 的「M0 不做原文并排」**改为「M0 不做证据图面板与区域高亮」**——`evidence_text` 只是一个字符串列，多渲染一列成本近乎为零，而它是闸门 2 的最小兑现，推迟它等于让 M0 少验一道闸门 |
| v0.3 | 2026-08-07 | **开放问题 P2 关闭：本位币可切换**（产品决定）。理由：固定单一本位币等于在 schema 层重新引入地域假设，与 v0.2 确立的 §3.1「解析能力与国家/币种无关」矛盾。切换语义由 [ADR-0004](./adr/0004-data-model-sqlite-integer-money.md)「折算冻结在交易发生那一刻」推导，未引入新原则——`base_currency` 逐笔冻结 / 切换不改历史行 / 跨切换点汇总按本位币分组不相加。同步：§5.1、§13；[ADR-0004](./adr/0004-data-model-sqlite-integer-money.md)（三元组含本位币种，文末未决关闭）、[00 地基](./prd/00-foundation.md) v0.2、[04 交易](./prd/04-transactions.md) v0.3、[INDEX](./prd/INDEX.md) v0.3、[`docs/CONTEXT.md`](./CONTEXT.md)、`.claude/rules/money-and-data.md` |
| v0.2 | 2026-08-07 | **定位修正**（产品决定，三条均成立）：① §2 主用户从「当场记不了只能事后补的人」改为「想记账但坚持不下来的人」——「事后补」是使用常态而非用户细分，原写法把问题写小了；② §1/§2/§3.1/§10 取消跨境双币种（澳洲 + 微信支付宝）作为「第一优先场景」的定位——多币种/多渠道是 §3.1 能力的自然结果而非市场边界，该组合改为 dogfooding 压力测试场景；「国内 app 读不懂澳洲对账单」由「市场夹缝」改述为「专用解析器路线的必然结果」；③ 全文移除立项阶段的叙述痕迹（「三个支点」「生死线」「需要清醒接受的三件事」等），改为产品文档语言 |
