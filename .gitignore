# ── Node / 前端 ──────────────────────────────────────────
node_modules/
dist/
dist-ssr/
.vite/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Rust / Tauri ─────────────────────────────────────────
/target/
/src-tauri/target/
**/*.rs.bk
*.pdb

# ── macOS ────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# ── 编辑器 ────────────────────────────────────────────────
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# ── 本机数据：绝不入库 ────────────────────────────────────
# CLAUDE.md 约束 2「数据不出本机」在版本控制侧的对应物。
# 账本、证据截图、开发期样本一律留在工作副本里，不进 git、不进任何远端。
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
/evidence/
/.daybook-dev/
/samples/

# 夹具（docs/prd/07-eval.md §3.6/§3.7）：本机集与 CI 集必须分离。
#   fixtures/local/ — 随自用积累的真实截图与真实金额，绝不进 git
#   fixtures/ci/    — 手工脱敏或合成的一小撮，进仓库，只用于重放回归
# 因此这里只挡 local 一支；ci 一支需要能提交，故显式反挡。
/fixtures/local/
!/fixtures/ci/

.env
.env.*
!.env.example

# ── Claude Code 本地覆盖 ─────────────────────────────────
.claude/settings.local.json

# ── 本地 UI 验证产物 ─────────────────────────────────────
/.playwright-mcp/
/output/
